PORT=8080
STORAGE=sqlite
SQLITE_PATH=data/code-sprint.db
# Required (at least 24 characters) when NODE_ENV=production.
# ADMIN_KEY=replace-with-a-long-random-secret
# Cloud Run must use STORAGE=firestore.
